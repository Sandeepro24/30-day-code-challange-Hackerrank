#include <bits/stdc++.h>

using namespace std;



int main()
{
    int N;
    cin >> N;
    if(N%2!=0 || N%2==0 && N<=20 && N>=6)
    {
        cout<<"Weird";
    }
    else
    if(N%2==0 && N>20 || N%2==0 && N<=5 && N>=2)
    {
        cout<<"Not Weird";
    }

    return 0;
}
